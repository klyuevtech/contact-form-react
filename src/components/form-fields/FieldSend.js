import React from "react";

const FieldSend = (props) => {
    return (
        <div className="form-field">
            <button type="submit" className="submit-button">Send</button>
        </div>
    )
}

export default FieldSend;